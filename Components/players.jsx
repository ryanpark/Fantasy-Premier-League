import React from 'react';

var Players = React.createClass({
    
   render : function () {
       return (
           <span> {this.props.url}</span>
          
          );
   } 
});


export default Players;